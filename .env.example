# ╔═══════════════════════════════════════════════════════════════════════╗
# ║  Mico's Micro Farm — Environment Variables                          ║
# ║  Copy to .env and fill in your values. NEVER commit the .env file.  ║
# ╚═══════════════════════════════════════════════════════════════════════╝

# ── Frontend (Vite — must start with VITE_) ──────────────────────────
# Firebase project config (from Firebase Console → Project Settings)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=
# Firebase Cloud Messaging VAPID key (for push notifications)
VITE_FIREBASE_VAPID_KEY=

# ── Backend: Firebase Admin (Vercel serverless functions) ────────────
# Full JSON service account (used by api/_lib/firebaseAdmin.js)
FIREBASE_SERVICE_ACCOUNT=
# Individual fields (used by stripe-webhook.js if FIREBASE_SERVICE_ACCOUNT is not set)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# ── Backend: Shopify ─────────────────────────────────────────────────
SHOPIFY_STORE_DOMAIN=                   # e.g. your-store.myshopify.com
SHOPIFY_ADMIN_API_TOKEN=                # Admin API access token
SHOPIFY_CLIENT_ID=                      # App client ID (for OAuth callback)
SHOPIFY_CLIENT_SECRET=                  # App client secret (for OAuth callback)
SHOPIFY_WEBHOOK_SECRET=                 # Webhook HMAC verification secret

# ── Backend: Stripe ──────────────────────────────────────────────────
STRIPE_SECRET_KEY=                      # sk_live_... or sk_test_...
STRIPE_WEBHOOK_SECRET=                  # whsec_... (from Stripe dashboard)
STRIPE_PRICE_PRO=                       # price_... ($49/mo Pro plan)
STRIPE_PRICE_BUSINESS=                  # price_... ($149/mo Business plan)

# ── Backend: EasyRoutes ──────────────────────────────────────────────
EASYROUTES_WEBHOOK_SECRET=              # Webhook verification secret

# ── Backend: API Auth ────────────────────────────────────────────────
SYNC_API_SECRET=                        # Shared secret for sync API endpoints

# ── Netlify Functions (only needed if using Netlify) ─────────────────
RESEND_API_KEY=                         # Resend email API key
ADMIN_EMAIL=                            # Email to notify on access requests
